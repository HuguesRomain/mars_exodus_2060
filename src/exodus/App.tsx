import React from 'react';
import { GlobalStyled } from '../styles/global';
import { Normalize } from 'styled-normalize'

export const App = () => {
    return (
        <div>
            <Normalize />
        </div>
    )
}