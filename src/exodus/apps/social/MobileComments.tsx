import React from "react";

const MobileComments = () => {
  return (
    <main>
      <ul>
        {/* {posts &&
          posts.map((value) => {
            return <PostItem key={value.like} posts={value} />;
          })} */}
        <p>COMMENT PAGE FOR MOBILE HER</p>
      </ul>
    </main>
  );
};

export default MobileComments;
